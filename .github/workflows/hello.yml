name: 11.3

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: output hello
        run: |
          echo "Hello World!"
        
      - name: date
        run: |
          date
      
      - name: ls
        run: |
          ls
